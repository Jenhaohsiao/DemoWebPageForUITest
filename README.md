# Playwright Sandbox

A lightweight, vanilla JS playground for E2E testing automation.

## Features
- **Automation Ready**: Unique, stable `id` attributes on all form elements.
- **Dynamic UI**: Cascading dropdowns and validation states.
- **Vanilla Tech**: No React, no build tools—just open `index.html`.
- **Comprehensive Fields**: Text, email, date, radio, checkbox, and long text.

## Usage
Simply open `index.html` in your browser. All logic is contained in `app.js`.